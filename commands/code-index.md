---
description: Re-index this repo for intent-aware code search
---

Re-index this repository so code search is current. Call the `code_index` MCP
tool (from the `code` server), then briefly report how many symbols were added,
changed, or removed, and how many files were skipped.
